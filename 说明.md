# YY Music 完整源码（含 UI 层）

本工程 = 原始「非 UI 源码」+ 本次补全的整套 SwiftUI 液态玻璃 UI 层，可完整构建。

## 包含内容
- 核心逻辑层：Models / NetEaseAPI / NetEaseCrypto / PlayerManager / AuthStore / Theme
- UI 层（本次新增）：YYMusicApp / RootView / DiscoverView / SearchView / LibraryView /
  PlaylistView / PlayerView / MiniPlayerView / QueueView / HistoryView / LoginView /
  SimiSongsSheet / SleepTimerSheet / AddToPlaylistSheet / SongCell / Components
- 工程配置：project.yml、Assets.xcassets、CI 工作流（build-unsigned-ipa.yml）
- 文档：README.md、FEATURES.md（79 项功能清单）

## 构建
见 README.md：macOS + Xcode 26 + xcodegen 本地构建，或推 GitHub Actions 云端打包未签名 IPA。

## 说明
- 要求 iOS 26+（液态玻璃 .glassEffect）
- 网易云非官方接口，仅供个人自用